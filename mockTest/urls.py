from django.urls import path
# from .views import GetQuiz

urlpatterns = [
    # path("csv/", GetQuiz.as_view(), name="file uploader view"),
    # path("execute/", ImportSubject, name="file uploader view"),
]